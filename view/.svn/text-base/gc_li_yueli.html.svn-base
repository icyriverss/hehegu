
<table class="data-load"  border="0" cellspacing="0" cellpadding="0" ><thead>
    <tr>
        <th colspan="3" rowspan="2">能源消耗状况月历表</th>
        {foreach $list as $id=>$info}
        <th>{$info['month']}</th>
        {/foreach}
    </tr>
    <tr>
        {foreach $list as $id=>$info}
        <th>{$info['fest']}</th>
        {/foreach}
        </tr>
</thead>
<tbody>
    <tr>
        <th colspan="3">销售天数（天）</th>
        {foreach $list as $id=>$info}
        <td>{$info['days']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="3">本期销售额（元）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['total'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th rowspan="11" width="50" >用电</th>
        <th rowspan="3" width="100">用电量（度）</th>
        <th width="100">厨房</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['chufang'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th>餐厅</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['dating'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th>小计</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['dian_total'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">100元用电量（度）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['100元用电量（度）'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">日均用电量（度/天）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['日均用电量（度/天）'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">电费单价（元/度）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['dian_price'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th rowspan="3">用电量（元）</th>
        <th>厨房</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['chufang_cost'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th>餐厅</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['dating_cost'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th>小计</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['dian_cost_total'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">100元用电量（天）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['100元用电量（天）'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">日均用电量（元/天）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['日均用电量（元/天）'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th rowspan="7">用水</th>
        <th colspan="2">用水量（吨）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['yongshui'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">100元用水量（吨）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['100元用水量（吨）'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">日均用水量（吨/天）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['日均用水量（吨/天）'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">水费单价（元/吨）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['shui_price'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">用水量（元）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['shui_cost'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">100元用水量（天）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['100元用水量（天）'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">日均用水量（元/天）</th>
        {foreach $list as $id=>$info}
        <td>{dot($info['日均用水量（元/天）'],1)}</td>
        {/foreach}
    </tr>
    <tr>
        <th rowspan="7">用气</th>
        <th colspan="2">用气量（立方）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">100元用气量（立方）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">日均用气量（立方/天）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">气费单价（元/立方）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">用气量（元）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">100元用气量（元）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">日均用气量（元/天）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th rowspan="2">其他</th>
        <th colspan="2">采暖制冷等（元）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="2">100元采暖制冷等能源（元）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="3">总能耗（元）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="3">100元能耗（元）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="3">日均能耗量（元/天）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="3">年平均天气温度（度）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
    <tr>
        <th colspan="3">安全（停水/停电/设备停用等）（天）</th>
        {foreach $list as $id=>$info}
        <td>{$info['luyong']}</td>
        {/foreach}
    </tr>
</tbody></table>

