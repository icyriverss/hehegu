<table class="data-load" border="0" cellspacing="0" cellpadding="0" width="1600">
    <tr>
        <th style="background: #dedede; border-bottom: 1px solid #ccc; height: 36px; text-align: left; padding-left: 10px;" width="100%">
            <span style="float: left;">选择部门：
                <select name="department">
                    {foreach $departments as $k=>$v}
                    <option value="{$v}" {if $department==$v}selected{/if}>{$v}</option>
                    {/foreach}
                    </select>
                  双击单元格即可编辑该单元格数据，修改后点击任意空白处即可保存。
            </span>
        </th>
    </tr>
    <tr>
        <th width="100%">
            <div id="itemslist" style="width:100%;"></div>
        </th>
    </tr>
</table>
